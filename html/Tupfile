.gitignore

include_rules

CPPFLAGS += -fPIC -I ../src -I/usr/include/python2.7

LDFLAGS += -shared -lpython2.7 -lboost_python -lncurses

!htmllinker = |> ^ HTML LD^ $(LD) $(LDFLAGS) $(SHARED_LIBS) -o %o %f |> pytungsten.so

: foreach *.cpp |> !cxx |>
: *.o ../src/*.o |> !htmllinker |>
