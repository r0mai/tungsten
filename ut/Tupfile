.gitignore

include_rules

LIBS += $(boost_lib_dir)/libboost_unit_test_framework.a

: foreach ast/*.cpp eval/*.cpp io/*.cpp eval/builtin/*.cpp math/*.cpp *.cpp |> !cxx |>
: *.o ../src/*.o |> !linker |> unitTest
