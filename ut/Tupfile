.gitignore

include_rules

BOOST_LIBS += $(boost_lib_dir)/libboost_unit_test_framework.a

: foreach ast/*.cpp eval/*.cpp eval/builtin/*.cpp math/*.cpp util/*.cpp *.cpp |> !cxx |>
: *.o ../src/*.o |> !linker |> unitTest
